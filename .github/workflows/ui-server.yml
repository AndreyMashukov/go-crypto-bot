name: ui/server (Symfony backend)

on:
  push:
    branches: [main]
    paths:
      - 'ui/server/**'
      - '.github/workflows/ui-server.yml'
  pull_request:
    paths:
      - 'ui/server/**'
      - '.github/workflows/ui-server.yml'

permissions:
  contents: read

defaults:
  run:
    working-directory: ui/server

env:
  # The php-parallax extension is required at runtime but lives only inside
  # the project's ZTS Docker image; CI runs on shivammathur/setup-php which
  # is NTS, so composer needs to know not to fail the platform check.
  COMPOSER_FLAGS: '--prefer-dist --no-progress --no-interaction --ignore-platform-req=ext-parallax'

jobs:
  composer:
    name: composer resolve
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none
          tools: composer:v2
          extensions: bcmath, gd, iconv, intl, mbstring, opcache, pcntl, pdo_pgsql, sockets, xsl, zip
      - name: resolve deps
        run: |
          if [ -f composer.lock ]; then
            composer install $COMPOSER_FLAGS --ansi
          else
            composer update $COMPOSER_FLAGS --no-scripts --ansi
          fi

  cs-fixer:
    name: php-cs-fixer --dry-run
    runs-on: ubuntu-latest
    needs: composer
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none
          tools: composer:v2
          extensions: bcmath, gd, iconv, intl, mbstring, opcache, pcntl, pdo_pgsql, sockets, xsl, zip
      - run: composer install $COMPOSER_FLAGS
      - run: vendor/bin/php-cs-fixer fix --dry-run --diff --config=.php-cs-fixer.dist.php --allow-risky=yes

  phpstan:
    name: phpstan
    runs-on: ubuntu-latest
    needs: composer
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none
          tools: composer:v2
          extensions: bcmath, gd, iconv, intl, mbstring, opcache, pcntl, pdo_pgsql, sockets, xsl, zip
      - run: composer install $COMPOSER_FLAGS
      - run: vendor/bin/phpstan analyse --memory-limit=1G --no-progress

  rector:
    name: rector --dry-run
    runs-on: ubuntu-latest
    needs: composer
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none
          tools: composer:v2
          extensions: bcmath, gd, iconv, intl, mbstring, opcache, pcntl, pdo_pgsql, sockets, xsl, zip
      - run: composer install $COMPOSER_FLAGS
      - run: vendor/bin/rector process --dry-run --no-progress-bar

  phpunit:
    name: phpunit (functional + unit, PostgreSQL 16)
    runs-on: ubuntu-latest
    needs: composer
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: crypto_saas
          POSTGRES_PASSWORD: crypto_saas
          POSTGRES_DB: crypto_saas_test
        ports: ['5432:5432']
        options: >-
          --health-cmd="pg_isready -U crypto_saas -d crypto_saas_test"
          --health-interval=5s
          --health-timeout=3s
          --health-retries=10
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none
          tools: composer:v2
          extensions: bcmath, gd, iconv, intl, mbstring, opcache, pcntl, pdo_pgsql, sockets, xsl, zip
      - run: composer install $COMPOSER_FLAGS
      - env:
          APP_ENV: test
          DATABASE_URL: postgresql://crypto_saas:crypto_saas@127.0.0.1:5432/crypto_saas_test?serverVersion=16&charset=utf8
        run: vendor/bin/phpunit -c phpunit.xml.dist --no-coverage
